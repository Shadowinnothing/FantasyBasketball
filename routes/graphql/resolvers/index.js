// Resolvers (functions to run various actions)
const resolvers = {
    hello: () => {
        return "Hello World! KIss my ass"
    },
    dreesen: () => {
        return "Dimes all day till we gross 100k Dreesen"
    },
    test: () => {
        return true
    }
}

module.exports = resolvers